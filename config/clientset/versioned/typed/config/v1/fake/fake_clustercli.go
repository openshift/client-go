// Code generated by client-gen. DO NOT EDIT.

package fake

import (
	configv1 "github.com/openshift/api/config/v1"
	v1 "k8s.io/apimachinery/pkg/apis/meta/v1"
	labels "k8s.io/apimachinery/pkg/labels"
	schema "k8s.io/apimachinery/pkg/runtime/schema"
	types "k8s.io/apimachinery/pkg/types"
	watch "k8s.io/apimachinery/pkg/watch"
	testing "k8s.io/client-go/testing"
)

// FakeClusterCLIs implements ClusterCLIInterface
type FakeClusterCLIs struct {
	Fake *FakeConfigV1
}

var clusterclisResource = schema.GroupVersionResource{Group: "config.openshift.io", Version: "v1", Resource: "clusterclis"}

var clusterclisKind = schema.GroupVersionKind{Group: "config.openshift.io", Version: "v1", Kind: "ClusterCLI"}

// Get takes name of the clusterCLI, and returns the corresponding clusterCLI object, and an error if there is any.
func (c *FakeClusterCLIs) Get(name string, options v1.GetOptions) (result *configv1.ClusterCLI, err error) {
	obj, err := c.Fake.
		Invokes(testing.NewRootGetAction(clusterclisResource, name), &configv1.ClusterCLI{})
	if obj == nil {
		return nil, err
	}
	return obj.(*configv1.ClusterCLI), err
}

// List takes label and field selectors, and returns the list of ClusterCLIs that match those selectors.
func (c *FakeClusterCLIs) List(opts v1.ListOptions) (result *configv1.ClusterCLIList, err error) {
	obj, err := c.Fake.
		Invokes(testing.NewRootListAction(clusterclisResource, clusterclisKind, opts), &configv1.ClusterCLIList{})
	if obj == nil {
		return nil, err
	}

	label, _, _ := testing.ExtractFromListOptions(opts)
	if label == nil {
		label = labels.Everything()
	}
	list := &configv1.ClusterCLIList{ListMeta: obj.(*configv1.ClusterCLIList).ListMeta}
	for _, item := range obj.(*configv1.ClusterCLIList).Items {
		if label.Matches(labels.Set(item.Labels)) {
			list.Items = append(list.Items, item)
		}
	}
	return list, err
}

// Watch returns a watch.Interface that watches the requested clusterCLIs.
func (c *FakeClusterCLIs) Watch(opts v1.ListOptions) (watch.Interface, error) {
	return c.Fake.
		InvokesWatch(testing.NewRootWatchAction(clusterclisResource, opts))
}

// Create takes the representation of a clusterCLI and creates it.  Returns the server's representation of the clusterCLI, and an error, if there is any.
func (c *FakeClusterCLIs) Create(clusterCLI *configv1.ClusterCLI) (result *configv1.ClusterCLI, err error) {
	obj, err := c.Fake.
		Invokes(testing.NewRootCreateAction(clusterclisResource, clusterCLI), &configv1.ClusterCLI{})
	if obj == nil {
		return nil, err
	}
	return obj.(*configv1.ClusterCLI), err
}

// Update takes the representation of a clusterCLI and updates it. Returns the server's representation of the clusterCLI, and an error, if there is any.
func (c *FakeClusterCLIs) Update(clusterCLI *configv1.ClusterCLI) (result *configv1.ClusterCLI, err error) {
	obj, err := c.Fake.
		Invokes(testing.NewRootUpdateAction(clusterclisResource, clusterCLI), &configv1.ClusterCLI{})
	if obj == nil {
		return nil, err
	}
	return obj.(*configv1.ClusterCLI), err
}

// UpdateStatus was generated because the type contains a Status member.
// Add a +genclient:noStatus comment above the type to avoid generating UpdateStatus().
func (c *FakeClusterCLIs) UpdateStatus(clusterCLI *configv1.ClusterCLI) (*configv1.ClusterCLI, error) {
	obj, err := c.Fake.
		Invokes(testing.NewRootUpdateSubresourceAction(clusterclisResource, "status", clusterCLI), &configv1.ClusterCLI{})
	if obj == nil {
		return nil, err
	}
	return obj.(*configv1.ClusterCLI), err
}

// Delete takes name of the clusterCLI and deletes it. Returns an error if one occurs.
func (c *FakeClusterCLIs) Delete(name string, options *v1.DeleteOptions) error {
	_, err := c.Fake.
		Invokes(testing.NewRootDeleteAction(clusterclisResource, name), &configv1.ClusterCLI{})
	return err
}

// DeleteCollection deletes a collection of objects.
func (c *FakeClusterCLIs) DeleteCollection(options *v1.DeleteOptions, listOptions v1.ListOptions) error {
	action := testing.NewRootDeleteCollectionAction(clusterclisResource, listOptions)

	_, err := c.Fake.Invokes(action, &configv1.ClusterCLIList{})
	return err
}

// Patch applies the patch and returns the patched clusterCLI.
func (c *FakeClusterCLIs) Patch(name string, pt types.PatchType, data []byte, subresources ...string) (result *configv1.ClusterCLI, err error) {
	obj, err := c.Fake.
		Invokes(testing.NewRootPatchSubresourceAction(clusterclisResource, name, pt, data, subresources...), &configv1.ClusterCLI{})
	if obj == nil {
		return nil, err
	}
	return obj.(*configv1.ClusterCLI), err
}
