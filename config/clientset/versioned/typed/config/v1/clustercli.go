// Code generated by client-gen. DO NOT EDIT.

package v1

import (
	"time"

	v1 "github.com/openshift/api/config/v1"
	scheme "github.com/openshift/client-go/config/clientset/versioned/scheme"
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
	types "k8s.io/apimachinery/pkg/types"
	watch "k8s.io/apimachinery/pkg/watch"
	rest "k8s.io/client-go/rest"
)

// ClusterCLIsGetter has a method to return a ClusterCLIInterface.
// A group's client should implement this interface.
type ClusterCLIsGetter interface {
	ClusterCLIs() ClusterCLIInterface
}

// ClusterCLIInterface has methods to work with ClusterCLI resources.
type ClusterCLIInterface interface {
	Create(*v1.ClusterCLI) (*v1.ClusterCLI, error)
	Update(*v1.ClusterCLI) (*v1.ClusterCLI, error)
	UpdateStatus(*v1.ClusterCLI) (*v1.ClusterCLI, error)
	Delete(name string, options *metav1.DeleteOptions) error
	DeleteCollection(options *metav1.DeleteOptions, listOptions metav1.ListOptions) error
	Get(name string, options metav1.GetOptions) (*v1.ClusterCLI, error)
	List(opts metav1.ListOptions) (*v1.ClusterCLIList, error)
	Watch(opts metav1.ListOptions) (watch.Interface, error)
	Patch(name string, pt types.PatchType, data []byte, subresources ...string) (result *v1.ClusterCLI, err error)
	ClusterCLIExpansion
}

// clusterCLIs implements ClusterCLIInterface
type clusterCLIs struct {
	client rest.Interface
}

// newClusterCLIs returns a ClusterCLIs
func newClusterCLIs(c *ConfigV1Client) *clusterCLIs {
	return &clusterCLIs{
		client: c.RESTClient(),
	}
}

// Get takes name of the clusterCLI, and returns the corresponding clusterCLI object, and an error if there is any.
func (c *clusterCLIs) Get(name string, options metav1.GetOptions) (result *v1.ClusterCLI, err error) {
	result = &v1.ClusterCLI{}
	err = c.client.Get().
		Resource("clusterclis").
		Name(name).
		VersionedParams(&options, scheme.ParameterCodec).
		Do().
		Into(result)
	return
}

// List takes label and field selectors, and returns the list of ClusterCLIs that match those selectors.
func (c *clusterCLIs) List(opts metav1.ListOptions) (result *v1.ClusterCLIList, err error) {
	var timeout time.Duration
	if opts.TimeoutSeconds != nil {
		timeout = time.Duration(*opts.TimeoutSeconds) * time.Second
	}
	result = &v1.ClusterCLIList{}
	err = c.client.Get().
		Resource("clusterclis").
		VersionedParams(&opts, scheme.ParameterCodec).
		Timeout(timeout).
		Do().
		Into(result)
	return
}

// Watch returns a watch.Interface that watches the requested clusterCLIs.
func (c *clusterCLIs) Watch(opts metav1.ListOptions) (watch.Interface, error) {
	var timeout time.Duration
	if opts.TimeoutSeconds != nil {
		timeout = time.Duration(*opts.TimeoutSeconds) * time.Second
	}
	opts.Watch = true
	return c.client.Get().
		Resource("clusterclis").
		VersionedParams(&opts, scheme.ParameterCodec).
		Timeout(timeout).
		Watch()
}

// Create takes the representation of a clusterCLI and creates it.  Returns the server's representation of the clusterCLI, and an error, if there is any.
func (c *clusterCLIs) Create(clusterCLI *v1.ClusterCLI) (result *v1.ClusterCLI, err error) {
	result = &v1.ClusterCLI{}
	err = c.client.Post().
		Resource("clusterclis").
		Body(clusterCLI).
		Do().
		Into(result)
	return
}

// Update takes the representation of a clusterCLI and updates it. Returns the server's representation of the clusterCLI, and an error, if there is any.
func (c *clusterCLIs) Update(clusterCLI *v1.ClusterCLI) (result *v1.ClusterCLI, err error) {
	result = &v1.ClusterCLI{}
	err = c.client.Put().
		Resource("clusterclis").
		Name(clusterCLI.Name).
		Body(clusterCLI).
		Do().
		Into(result)
	return
}

// UpdateStatus was generated because the type contains a Status member.
// Add a +genclient:noStatus comment above the type to avoid generating UpdateStatus().

func (c *clusterCLIs) UpdateStatus(clusterCLI *v1.ClusterCLI) (result *v1.ClusterCLI, err error) {
	result = &v1.ClusterCLI{}
	err = c.client.Put().
		Resource("clusterclis").
		Name(clusterCLI.Name).
		SubResource("status").
		Body(clusterCLI).
		Do().
		Into(result)
	return
}

// Delete takes name of the clusterCLI and deletes it. Returns an error if one occurs.
func (c *clusterCLIs) Delete(name string, options *metav1.DeleteOptions) error {
	return c.client.Delete().
		Resource("clusterclis").
		Name(name).
		Body(options).
		Do().
		Error()
}

// DeleteCollection deletes a collection of objects.
func (c *clusterCLIs) DeleteCollection(options *metav1.DeleteOptions, listOptions metav1.ListOptions) error {
	var timeout time.Duration
	if listOptions.TimeoutSeconds != nil {
		timeout = time.Duration(*listOptions.TimeoutSeconds) * time.Second
	}
	return c.client.Delete().
		Resource("clusterclis").
		VersionedParams(&listOptions, scheme.ParameterCodec).
		Timeout(timeout).
		Body(options).
		Do().
		Error()
}

// Patch applies the patch and returns the patched clusterCLI.
func (c *clusterCLIs) Patch(name string, pt types.PatchType, data []byte, subresources ...string) (result *v1.ClusterCLI, err error) {
	result = &v1.ClusterCLI{}
	err = c.client.Patch(pt).
		Resource("clusterclis").
		SubResource(subresources...).
		Name(name).
		Body(data).
		Do().
		Into(result)
	return
}
