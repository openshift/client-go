// Code generated by lister-gen. DO NOT EDIT.

package v1

import (
	v1 "github.com/openshift/api/config/v1"
	"k8s.io/apimachinery/pkg/api/errors"
	"k8s.io/apimachinery/pkg/labels"
	"k8s.io/client-go/tools/cache"
)

// DeschedulerLister helps list Deschedulers.
// All objects returned here must be treated as read-only.
type DeschedulerLister interface {
	// List lists all Deschedulers in the indexer.
	// Objects returned here must be treated as read-only.
	List(selector labels.Selector) (ret []*v1.Descheduler, err error)
	// Get retrieves the Descheduler from the index for a given name.
	// Objects returned here must be treated as read-only.
	Get(name string) (*v1.Descheduler, error)
	DeschedulerListerExpansion
}

// deschedulerLister implements the DeschedulerLister interface.
type deschedulerLister struct {
	indexer cache.Indexer
}

// NewDeschedulerLister returns a new DeschedulerLister.
func NewDeschedulerLister(indexer cache.Indexer) DeschedulerLister {
	return &deschedulerLister{indexer: indexer}
}

// List lists all Deschedulers in the indexer.
func (s *deschedulerLister) List(selector labels.Selector) (ret []*v1.Descheduler, err error) {
	err = cache.ListAll(s.indexer, selector, func(m interface{}) {
		ret = append(ret, m.(*v1.Descheduler))
	})
	return ret, err
}

// Get retrieves the Descheduler from the index for a given name.
func (s *deschedulerLister) Get(name string) (*v1.Descheduler, error) {
	obj, exists, err := s.indexer.GetByKey(name)
	if err != nil {
		return nil, err
	}
	if !exists {
		return nil, errors.NewNotFound(v1.Resource("descheduler"), name)
	}
	return obj.(*v1.Descheduler), nil
}
